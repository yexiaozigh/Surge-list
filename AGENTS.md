# Surge-list任务执行规范

- 本仓库是自维护补充规则集，不复制第三方规则全集。
- 文件名和GitHub URL是现行生产接口；修改前检查Surge、Hub及规则镜像消费者，不改名、不移动。
- 规则顺序具有行为意义。不得自动排序、按语义去重、扩展`DOMAIN-SUFFIX`或用第三方清单覆盖用户规则。
- lint只做结构检查：基本语法、意外粘行、完全重复项、文件末尾换行和明显非法类型；lint不得改写规则。
- 不提交订阅URL、token、密码、密钥、节点凭据或其他秘密。
- 修改规则时只处理已确认的问题，运行`python3 scripts/lint_rules.py`及直接相关测试后停止。
- 未经明确授权，不改仓库名、默认分支、远端地址，不强推、不重写历史、不删除远端分支。
